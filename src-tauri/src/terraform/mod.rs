// Terraform orchestration. Invokes the bundled, hash-verified Terraform
// binary by absolute path with an argv array — never through a shell, never
// with interpolated strings. See CLAUDE.md §4.5 and §5.
//
// Filled in by Contract 05 (Terraform Provisioner).
