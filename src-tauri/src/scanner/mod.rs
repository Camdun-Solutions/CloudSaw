// Scanner orchestration. Spawns the bundled, hash-verified ScoutSuite binary
// by absolute path with an argv array, captures normalized JSON output, and
// hands it to `findings`. Never invokes through a shell.
//
// Filled in by Contract 06 (Scanner Orchestrator).
