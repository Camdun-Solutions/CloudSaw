// Findings parser & store. Normalizes scanner output and persists into the
// local SQLite database. Account IDs are stored full-length here; redaction
// happens at the logging boundary, not at rest. See CLAUDE.md §4.4.
//
// Filled in by Contract 07 (Findings Parser & Store).
