// AWS authentication. Resolves credentials via the AWS SDK provider chain;
// CloudSaw code never reads ~/.aws/credentials directly. Long-term secret
// access keys are NEVER accepted from the UI. See CLAUDE.md §4.3 and §5.
//
// Filled in by Contract 03 (AWS Auth).
